{
  "name": "functions",
  "scripts": {
    "build": "tsc && cp src/ai_composition_guidelines.json lib/ && cp -r catalogs lib/",
    "build:watch": "tsc --watch",
    "serve": "npm run build && firebase emulators:start --only functions",
    "shell": "npm run build && firebase functions:shell",
    "start": "npm run shell",
    "deploy": "firebase deploy --only functions",
    "deploy:prod": "firebase deploy --only functions",
    "deploy:dev": "firebase use dev && firebase deploy --only functions",
    "deploy:dev-to-prod": "firebase use default && firebase deploy --only functions",
    "logs": "firebase functions:log"
  },
  "engines": {
    "node": "20"
  },
  "main": "lib/index.js",
  "dependencies": {
    "firebase-admin": "^12.0.0",
    "firebase-functions": "^5.0.0"
  },
  "devDependencies": {
    "typescript": "^5.3.0",
    "@types/node": "^18.0.0"
  },
  "private": true
}
