# Google Analytics 4 — Web data stream Measurement ID (see GA4 Admin → Data streams).
# Optional for local dev. Production builds load .env.production automatically.
VITE_GA_MEASUREMENT_ID=G-XXXXXXXXXX
